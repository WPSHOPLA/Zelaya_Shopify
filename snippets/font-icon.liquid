<i class="{{ class }} {{ icon | remove: 'ion-' |  prepend: 'ion-' }} icon-size-{% if size %}{{ size }}{% else %}4x{% endif %}" aria-hidden="true" {% if title %}title="{{ title | strip_html | escape }}"{% endif %}></i>
